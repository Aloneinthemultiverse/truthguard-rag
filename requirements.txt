# core
anthropic>=0.40.0
openai>=1.40.0          # OpenAI-compatible providers: NVIDIA NIM, Ollama, vLLM
python-dotenv>=1.0.0
numpy>=1.24.0
networkx>=3.0
requests>=2.31.0

# api + studio backend  (uvicorn is the server; nothing imports it directly)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
httpx>=0.27.0
python-multipart>=0.0.9  # file upload on /ingest/document

# mcp server
mcp>=1.0.0

# ingestion
pdfplumber>=0.10.0
pypdfium2>=4.0.0
fpdf2>=2.7.0          # corpus generator only
pillow>=10.0.0
pytesseract>=0.3.10   # + Tesseract binary: winget install UB-Mannheim.TesseractOCR
# python-doctr         # optional Tier-1 engine: better on poor scans and
                       # multi-column reading order (TG_OCR_ENGINE=doctr)
# paddleocr            # unavailable: no paddlepaddle wheel for Python 3.13+
unstructured>=0.18.0    # docx/pptx/xlsx/html/epub/odt structural parsing
# markitdown           # optional fallback converter for docx/pptx

# retrieval
sentence-transformers>=2.2.0
rank-bm25>=0.2.2
# turbovec             # optional quantized index (numpy fallback built in)

# legacy dg-core package deps
python-louvain>=0.16
scikit-learn>=1.3.0
